// 使用 connect httpProxy crossroads
-
constlog = console.log.bind(console)

const __main = () => {
    var http = require('http'),
        proxySever = require('http-proxy'),
        fs = require('fs'),
        base = 'D:/webroot',
        connect = require('connect'),
        app = connect(),
        crossroads = require('crossroads'),



}
const __mainTest = () => {

}
