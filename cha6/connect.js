const log = console.log.bind(console)

const __main = () => {
    // connect static
    var connect = require('connect'),
        http = require('http'),
        base = 'D:/webroot',
        logger = require('morgan')

    // http.createServer(function(req, res){
    //     // connect().use(connect.static(base), {redirect: true})
    // }).listen(8124)
    log('run in 8124')
}
const __mainTest = () => {

}
__main()
// __mainTest()
